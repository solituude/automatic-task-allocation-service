const initialState = {
    login: '',
    password: '',
    role: '',
    isFetching: false,
}

export const appReducer = (state = initialState, action) => {
    switch (action.type) {


        default:
            return state;
    }
}